# AgentLight — local inference / eval deps.
# On Kaggle the notebook installs unsloth + trl fresh (see kaggle/run.py);
# this file targets a local machine for chat + evaluation against a checkpoint.
torch>=2.3
transformers>=4.44
peft>=0.12
accelerate>=0.33
datasets>=2.20
trl>=0.11
# unsloth is optional locally (needed for training, heavy on Windows).
# Install per https://github.com/unslothai/unsloth for your CUDA version.
